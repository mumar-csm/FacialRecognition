# Raspberry Pi (aarch64) requirements for the kiosk runtime.
#
# Differences from requirements.txt:
#   - opencv-python      → opencv-python-headless  (better ARM wheel availability,
#                                                   no GUI deps, kiosk UI is web-based)
#   - dlib               → REMOVED  (compiles from source on ARM ~30 min;
#                                    only used by DlibEmbedder, kiosk uses ArcFace)
#   - face-recognition   → REMOVED  (wraps dlib, same reason)
#   - matplotlib         → REMOVED  (only used by tune_threshold.py, not at runtime)
#   - scikit-learn       → REMOVED  (only used by tune_threshold.py, not at runtime)
#
# If you ever need the analysis tools or dlib embedder on the Pi, install from
# requirements.txt instead and budget ~45 min for the dlib compile.

opencv-python-headless>=4.5.0
numpy>=1.20.0,<2.0
Pillow>=8.0.0

# Detection/embedding (RetinaFace + ArcFace via InsightFace)
insightface>=0.7.3
onnxruntime>=1.14.0
scikit-image>=0.19.0
# onnx >=1.18 (pulled in by insightface) references ml_dtypes.float4_e2m1fn,
# but pip can resolve an older ml_dtypes that lacks it -> AttributeError at
# import. Verified working on Pi: onnx 1.19.0 + ml_dtypes 0.5.4.
ml_dtypes>=0.5

# Kiosk web server
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# Central sync (sync_worker.py / roster_client.py, imported by kiosk_server.py)
httpx>=0.25.0