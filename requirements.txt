opencv-python>=4.5.0
numpy>=1.20.0,<2.0
face-recognition>=1.3.0
dlib==19.24.2
Pillow>=8.0.0

# Analysis tools (optional — only needed for tune_threshold.py)
matplotlib>=3.5.0
scikit-learn>=1.0.0

# Phase 3 — Modern detection/embedding (RetinaFace via InsightFace)
insightface>=0.7.3
onnxruntime>=1.14.0
scikit-image>=0.19.0

# Phase K2 — Kiosk web server
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# Sync substrate — async HTTP client used by sync_worker.py to upload
# outbox events to central HQ
httpx>=0.25.0
